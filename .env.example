# Research OS — Semantic Scholar API Key
#
# Get a free key at: https://api.semanticscholar.org/
#
# Without this key, the system falls back to OpenAlex (no key needed)
# and unauthenticated Semantic Scholar requests (heavily rate-limited).
#
# S2_API_KEY=your_key_here
