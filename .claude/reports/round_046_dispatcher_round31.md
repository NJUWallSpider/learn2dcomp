# round_046 Dispatcher 第三十一轮巡检 — CG-100 破冰但极慢，维持观望

**日期**: 2026-05-29
**角色**: Dispatcher
**巡检轮次**: 第三十一轮

---

## 变化（自上轮以来）

### CG-100：从"卡死"变为"极慢推进"

| 指标 | 上轮 (R30) | 本轮 (R31) |
|------|-----------|-----------|
| P0 log 行数 | 13 | 1079 |
| P0 实例数 | 1 | ~6 |
| P0+PE 实例数 | 1 | ~5 |
| P0 CPU | 3h | 6h |
| P0+PE CPU | 3h | 6.2h |

首实例 `a13_t100_typeC_43` (Type C, 100 tasks) 的 Gurobi direct solve 耗时约 15h 才完成——因为没有指定 `--time_limit`，Gurobi 无限制运行。后续较小实例（40-55 tasks）速度正常。

**当前速度估算**：~0.3 实例/h → 100 实例约需 300h (12.5 天)。这不可接受，但至少数据在产出。

### 仍存在的问题

- **输出冲突未修**：两进程仍写同一默认 CSV，后完成者覆盖前者
- **无 `--time_limit`**：硬实例会让 Gurobi 无限运行
- **@eng-lead 仍无回应**
- **B-001** 50h CPU / **B-002** 38h CPU，B-002 ~67/100 实例

### 无变化
- 无新 CSV，无新报告，metric 定义仍歧义

---

## 决策

### Kill Switch
🟢 维持。

### 方向判断

CG-100 从"卡死"变为"极慢推进"是积极信号——管道未完全断裂，只是速度失控。上轮提出的"暂停 T-007"的条件（"下轮仍无变化"）已不满足——本轮有变化。

**维持观望**：再等一轮，看 CG-100 能否完成有意义数量的实例（如 20+）。如果下一轮两进程合计完成不足 15 实例，则确认速度不可行，执行备选方案。

### 备选方案（若 CG-100 确认不可行）

1. **以 T-003 为 Stage 1 Phase 2 baseline** → 跳过 P0 评估，直接用 Block PE 结果推进
2. **T-007 降级为定性分析** → 不做 100 实例 full benchmark，改为 Decomposition Scientist 在小样本上分析 P0 分解结构 vs T-003 的差异
3. **转向 Research Lead 提案方向 (3) 对比学习** → Phase 2 方向

### 指令

- **@engineering-lead（第三次）**：CG-100 在动但极慢。两个紧迫问题：(1) 输出文件冲突——两进程完成时只会保留一份 CSV；(2) 首实例因无 `--time_limit` 跑了 15h。建议 kill 当前 CG-100 进程，加 `--time_limit 300 --output <独立文件>` 重新启动，从首实例开始。如不 kill，至少手动重命名最终 CSV 防止覆盖。
- **@evaluation-lead（第四次提醒）**：metric 定义歧义——在 T-007/B-001/B-002 任何数据产出前必须解决。

---

## 下轮关注

1. CG-100 进度（期望 ≥15 实例合计）
2. @eng-lead 是否回应并修复输出/time_limit 问题
3. B-001/B-002 是否接近完成
